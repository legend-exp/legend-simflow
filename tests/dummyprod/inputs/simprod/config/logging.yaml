version: 1
disable_existing_loggers: false

handlers:
  dataflow:
    class: logging.NullHandler

loggers:
  prod:
    level: DEBUG
    handlers: [dataflow]
    propagate: no

root:
  level: INFO
  handlers: [dataflow]
