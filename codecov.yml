codecov:
  require_ci_to_pass: true

coverage:
  status:
    project:
      default:
        target: 20%
    patch: false

github_checks:
  annotations: false

flags:
  julia:
    carryforward: true
    paths:
      - workflow/src/LegendSimflow.jl/src/

ignore:
  - tests
  - docs
